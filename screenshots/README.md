# Screenshots Directory

Required screenshots for submission.

## Required Screenshots

1. `ec2-instance.png` - EC2 instance running in AWS Console
2. `security-groups.png` - Security group with port 5000 open
3. `docker-ps.png` - Output of `docker-compose ps`
4. `web-browser.png` - Application in browser at http://<EC2-IP>:5000
5. `curl-output.png` - Terminal showing `curl http://localhost:5000`
6. `api-health.png` - API health endpoint response
7. `cleanup.png` - Output of `docker-compose down --volumes`

## Tips

- Ensure text is readable
- Capture full terminal output
- Use browser developer tools for API responses
